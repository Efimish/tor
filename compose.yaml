services:
  tor:
    # build: .
    image: ghcr.io/efimish/tor
    container_name: tor
    restart: unless-stopped
    ports:
      - "127.0.0.1:9050:9050/tcp"
    # volumes:
    #   - ./torrc:/etc/tor/torrc
